C Program to 
-Accept file name and a string from user through command line argument
-Open the specified file
-Error if file cannot be opened / missing.
-Write the input string into the specified file.
-Close the file

